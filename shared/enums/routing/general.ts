export enum Operations{
    CREATE='create',
    READ='read',
    REPLACE='replace',
    DELETE='delete',
    UPDATE= 'update',
    INVOKE='invoke',
}

export enum HTTP_RESPOSE_CODES{
    CREATE=201,
    READ = 200,
    UPDATE = 200,
    DELETE = 204
}
